
import com.google.gson.Gson;
import controller.GameMenuController;
import controller.database.csvInfoGetter;
import model.card.CardFeatures;
import model.card.FeatureWrapper;
import model.game.Game;

import java.io.FileWriter;
import java.io.IOException;
import java.lang.reflect.Executable;
import java.util.ArrayList;

public class Main {
    public static void main(String[] args) throws IOException {
        // LoginMenu.getInstance().run();
//        Gson gson = new Gson();
//        ArrayList<String> names = csvInfoGetter.getCardNames();
//        FileWriter fileWriter = new FileWriter("src/resources/card-infos/features/" + "The Tricky" + ".json");
//        FeatureWrapper wrapper = new FeatureWrapper();
        //wrapper.features.add(CardFeatures.CANT_ATTACK_WHEN_FACE_UP);
        //wrapper.features.add(CardFeatures.CHANGING_ATK_DEF_POINTS_OF_CARDS);
        //wrapper.features.add(CardFeatures.DESTROY_ATTACKER);
        //wrapper.features.add(CardFeatures.DONT_CHANGE_LP);
        //wrapper.features.add(CardFeatures.ONE_EFFECT_PER_ROUND);
        //wrapper.features.add(CardFeatures.RITUAL_SUMMON);
        //wrapper.features.add(CardFeatures.FLIP_EFFECT);
        //wrapper.features.add(CardFeatures.NORMAL_SUMMON);
        //wrapper.features.add(CardFeatures.BANNING_TRAPS);
        //wrapper.features.add(CardFeatures.SPECIAL_EFFECT_WHEN_FACE_UP);
        //wrapper.features.add(CardFeatures.CHANGING_ATK_DEF_POINTS_OF_CARDS);
        //wrapper.features.add(CardFeatures.CANT_DESTROY_IN_ATTACK);
        //wrapper.features.add(CardFeatures.DESTROY_ATTACKER);
        //wrapper.features.add(CardFeatures.HAS_SPECIAL_NORMAL_SUMMON);
        //wrapper.features.add(CardFeatures.STOP_ATTACK);
        //wrapper.features.add(CardFeatures.HAS_SPECIAL_EFFECT_AFTER_DEFENDING);
        //wrapper.features.add(CardFeatures.HAS_SPECIAL_SUMMON_AFTER_NORMAL_SUMMON);
        //wrapper.features.add(CardFeatures.SCANNER);
//        wrapper.features.add(CardFeatures.SPECIAL_SUMMON);
//        gson.toJson(wrapper, fileWriter);
//        fileWriter.close();



    }
}